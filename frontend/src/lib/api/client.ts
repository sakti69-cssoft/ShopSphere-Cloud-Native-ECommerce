const API_BASE_URL=process.env.NEXT_PUBLIC_API_BASE_URL??"http://localhost/api/v1";const ACCESS_KEY="shopsphere-access",REFRESH_KEY="shopsphere-refresh";
export type ApiErrorBody={message?:string;validationErrors?:Record<string,string>};export class ApiError extends Error{constructor(public readonly status:number,message:string,public readonly correlationId?:string,public readonly validationErrors?:Record<string,string>){super(message);}}
export const tokenStore={access:()=>typeof window==="undefined"?null:sessionStorage.getItem(ACCESS_KEY),refresh:()=>typeof window==="undefined"?null:localStorage.getItem(REFRESH_KEY),set:(access:string,refresh:string)=>{sessionStorage.setItem(ACCESS_KEY,access);localStorage.setItem(REFRESH_KEY,refresh)},clear:()=>{sessionStorage.removeItem(ACCESS_KEY);localStorage.removeItem(REFRESH_KEY)}};
let refreshPromise:Promise<boolean>|null=null,unauthorizedHandler:(()=>void)|null=null;export const onUnauthorized=(handler:()=>void)=>{unauthorizedHandler=handler};
async function refreshAccess(){const refreshToken=tokenStore.refresh();if(!refreshToken)return false;if(!refreshPromise)refreshPromise=fetch(`${API_BASE_URL}/auth/refresh`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({refreshToken})}).then(async response=>{if(!response.ok)return false;const tokens=await response.json() as{accessToken:string;refreshToken:string};tokenStore.set(tokens.accessToken,tokens.refreshToken);return true}).catch(()=>false).finally(()=>{refreshPromise=null});return refreshPromise;}
export async function apiRequest<T>(path:string,init:RequestInit&{timeoutMs?:number;auth?:boolean;retry401?:boolean}={}):Promise<T>{const{timeoutMs=10000,auth=true,retry401=true,...requestInit}=init;const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);const headers=new Headers(requestInit.headers);if(requestInit.body&&!headers.has("Content-Type"))headers.set("Content-Type","application/json");const access=tokenStore.access();if(auth&&access)headers.set("Authorization",`Bearer ${access}`);try{const response=await fetch(`${API_BASE_URL}${path}`,{...requestInit,headers,signal:controller.signal,credentials:"omit"});if(response.status===401&&auth&&retry401&&await refreshAccess())return apiRequest<T>(path,{...init,retry401:false});const correlation=response.headers.get("X-Correlation-ID")??undefined;if(!response.ok){let body:ApiErrorBody={};try{body=await response.json()as ApiErrorBody}catch{}const defaults:Record<number,string>={400:"Please check the information and try again.",401:"Your session expired. Please sign in again.",403:"You do not have permission for this action.",404:"The requested item was not found.",409:"This request conflicts with existing data.",429:"Too many requests. Please wait and retry.",500:"Something went wrong.",503:"This service is temporarily unavailable."};if(response.status===401){tokenStore.clear();unauthorizedHandler?.()}throw new ApiError(response.status,body.message??defaults[response.status]??"ShopSphere API request failed",correlation,body.validationErrors)}if(response.status===204)return undefined as T;return await response.json()as T}catch(error){if(error instanceof ApiError)throw error;if(error instanceof DOMException&&error.name==="AbortError")throw new ApiError(408,"The request timed out. Please retry.");throw new ApiError(0,"Unable to connect to ShopSphere. Check your connection and retry.")}finally{clearTimeout(timer)}}
