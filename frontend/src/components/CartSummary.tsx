"use client";import Link from "next/link";import { money } from "@/data/mock";import { useStore } from "@/context/store";
export function CartSummary({checkout=false}:{checkout?:boolean}){const {cart}=useStore();const subtotal=cart.reduce((s,i)=>s+i.product.price*i.quantity,0);const mrp=cart.reduce((s,i)=>s+i.product.originalPrice*i.quantity,0);const delivery=subtotal>999?0:99;return <aside className="summary-card"><h2>Order summary</h2><div className="summary-line"><span>Subtotal ({cart.reduce((n,i)=>n+i.quantity,0)} items)</span><b>{money(subtotal)}</b></div><div className="summary-line"><span>Product savings</span><b style={{color:"var(--green)"}}>−{money(mrp-subtotal)}</b></div><div className="summary-line"><span>Delivery</span><b>{delivery?money(delivery):"FREE"}</b></div><div className="summary-line total"><span>Total</span><span>{money(subtotal+delivery)}</span></div><small>Inclusive of all taxes</small>{!checkout&&<Link className="btn-primary" style={{display:"block",textAlign:"center"}} href="/checkout">Proceed to checkout</Link>}</aside>}
