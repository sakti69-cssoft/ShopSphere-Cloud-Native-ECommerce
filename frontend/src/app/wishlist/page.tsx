"use client";import Link from "next/link";import { useStore } from "@/context/store";import { ProductCard } from "@/components/ProductCard";
export default function Wishlist(){const {wishlist}=useStore();return <div className="page-wrap"><h1 className="page-title">Your wishlist</h1>{wishlist.length?<div className="product-row">{wishlist.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><div className="symbol">♡</div><h2>Save the things you love</h2><p>Tap the heart on any product to keep it close.</p><Link className="btn-primary" href="/search">Discover products</Link></div>}</div>}
