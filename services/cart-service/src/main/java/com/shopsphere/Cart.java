package com.shopsphere;import java.math.BigDecimal;import java.time.Instant;import java.util.*;public record Cart(UUID userId,List<CartItem>items,BigDecimal subtotal,int itemCount,Instant updatedAt){static Cart empty(UUID id){return new Cart(id,List.of(),BigDecimal.ZERO,0,Instant.now());}}record CartItem(UUID productId,String productName,String imageUrl,BigDecimal unitPrice,int quantity,BigDecimal lineTotal){}
