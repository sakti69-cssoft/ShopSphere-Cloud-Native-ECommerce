# Copy only after provisioning your state bucket separately.
terraform {
  backend "s3" {
    bucket = "REPLACE_WITH_PRIVATE_STATE_BUCKET"
    key = "shopsphere/portfolio.tfstate"
    region = "ap-south-1"
    encrypt = true
    use_lockfile = true
  }
}
