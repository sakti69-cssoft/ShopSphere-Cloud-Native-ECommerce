import type{ApiProduct}from"./types";import type{Product}from"@/types";import{productImage}from"@/lib/product-images";export const toProduct=(p:ApiProduct):Product=>{const image=productImage(p);return{id:p.id,title:p.name,brand:p.brand,category:p.category,image,images:[image,...p.imageUrls.filter(url=>url!==image&&url!=="/media/cat-deals.jpg"&&url!=="/images/product.jpg")],rating:p.rating,reviewCount:p.reviewCount,originalPrice:p.originalPrice,price:p.price,badge:p.discountPercentage>=20?"Deal":undefined,delivery:"Free delivery on eligible orders",stock:0,description:p.description,specs:p.specifications}};
